set folder = ~/Mail/Personal/
set from = "rajesh@surf.net.np"
set realname = "Rajesh Sharma"
set spoolfile= "+INBOX"
set postponed= "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set nm_default_uri="notmuch:///Users/rajesh/Mail/Personal"
set virtual_spoolfile=yes

virtual-mailboxes \
        "INBOX"     "notmuch://?query=tag:inbox and NOT tag:archive"\
        "Archive"     "notmuch://?query=tag:archive"\
        "Unread"    "notmuch://?query=tag:unread"\
        "Starred"   "notmuch://?query=tag:*"\
        "Sent"      "notmuch://?query=tag:sent"

set header_cache = ~/.mutt/personal/cache/headers
set message_cachedir = ~/.mutt/personal/cache/bodies

set smtp_url = 'smtps://rajesh@surf.net.np@smtp.gmail.com'
set smtp_pass = `get-password personalmail`
# set ssl_force_tls = "yes"
# set smtp_authenticators = "login"
